$(function () {
    // $(document).on('click', '.notifications-section .read-at', function () {
    //     const notiId = $(this).data('noti-id')
    //     $.get(`${BASE_URL}notifications/${notiId}/mark-as-read`, (result, status) => {
    //         if (result.status) {
    //             const link = $(this).data('link')
    //             window.location.href = link;
    //         }
    //     });
    // })
})
